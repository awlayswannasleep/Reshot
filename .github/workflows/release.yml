name: Release

# Tag a commit to publish: git tag v1.0.0 && git push origin v1.0.0
# The release is created as a DRAFT, so the notes can be edited before it goes public.
on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to build (e.g. 1.0.0)'
        required: true

jobs:
  release:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: src/reshot-tauri/package-lock.json

      - uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src/reshot-tauri/src-tauri/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('src/reshot-tauri/src-tauri/Cargo.lock') }}

      - name: Resolve version
        id: version
        shell: pwsh
        run: |
          $v = '${{ github.event.inputs.version }}'
          if (-not $v) { $v = '${{ github.ref_name }}' -replace '^v', '' }
          "value=$v" >> $env:GITHUB_OUTPUT
          Write-Host "Building reshot $v"

      - name: Install Inno Setup
        run: choco install innosetup --no-progress -y

      - name: Build release artifacts
        shell: pwsh
        run: pwsh build/build-release.ps1 -Version ${{ steps.version.outputs.value }}

      - name: Upload artifacts to the workflow run
        uses: actions/upload-artifact@v4
        with:
          name: reshot-${{ steps.version.outputs.value }}
          path: |
            dist/*.zip
            dist/*.exe
            dist/SHA256SUMS.txt

      - name: Publish draft release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          name: reshot ${{ steps.version.outputs.value }}
          body_path: .github/RELEASE_TEMPLATE.md
          files: |
            dist/*.zip
            dist/*.exe
            dist/SHA256SUMS.txt
